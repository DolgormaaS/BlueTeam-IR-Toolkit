# BlueTeam-IR-Toolkit

To run these scripts, make sure you run Powershell with Admin permissions.
If it says you don't have permission to run scripts, run this:
```Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass```
Click 'y' and press enter when prompted. 
